#include<iostream>
#include<cstring>
#include<vector>
#include<cstdio>
#include<algorithm>
#include<climits>
#define MAX_N 1234
#define MAX_A 1000006
#define INF (INT_MAX-5)
using namespace std;

int N;
typedef long long ll;

ll a[MAX_N];

bool hash1[MAX_A];
bool hash2[MAX_A];

int main(){
	while(scanf("%d",&N)==1){
		if(N==0)break;

	}
	return 0;
}
