#include <string>
#include <algorithm>
#include <iostream>

std::string S;
int n;

int search_palindrome(int d, int start){
	for(int i = start; i + d <= n; i++){
		for(int j = 0;)
	}
	return -1;
}

int main(){
	std::cin >> S;
	n = S.length();

	for(int d = 2; d * d <= n; d++){
		if(n % d) continue;

	}
	return 0;
}