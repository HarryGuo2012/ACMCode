string 的erase很慢，就算是erase（s.begin()）也很慢
迷宫题注意判断起点与终点是不是在一起
计算连续小于某个值的区间，不能以大于那个值的数作为分割点计算区间长度，而应该边判断边加
线段树：更新非直接lazy区间要tree[pos]+=num*(min(qr,r)-max(ql-1,mid));
	注意pos*2不能越界，在叶子节点时
动规要求输出最小字典序时应该从右往左做。。。